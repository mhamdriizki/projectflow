services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: projectflow
      POSTGRES_PASSWORD: projectflow
      POSTGRES_DB: projectflow
    volumes:
      - projectflow_pgdata:/var/lib/postgresql/data

volumes:
  projectflow_pgdata: